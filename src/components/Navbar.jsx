import { useEffect, useState } from 'react'
import { FiMenu, FiX } from 'react-icons/fi'
const links = ['About', 'Education', 'Skills', 'Projects', 'Certifications', 'Contact']
function Navbar() { const [open, setOpen] = useState(false); const [active, setActive] = useState('about'); useEffect(() => { const observer = new IntersectionObserver((entries) => entries.forEach((entry) => entry.isIntersecting && setActive(entry.target.id)), { rootMargin: '-35% 0px -55% 0px' }); links.forEach((link) => { const section = document.getElementById(link.toLowerCase()); if (section) observer.observe(section) }); return () => observer.disconnect() }, []); return <header className="navbar"><a className="brand" href="#home">MANJUSHREE<span>.</span></a><button className="menu-toggle" onClick={() => setOpen(!open)} aria-label="Toggle navigation">{open ? <FiX /> : <FiMenu />}</button><nav className={open ? 'nav-links open' : 'nav-links'}>{links.map((link) => <a className={active === link.toLowerCase() ? 'active' : ''} key={link} href={`#${link.toLowerCase()}`} onClick={() => setOpen(false)}>{link}</a>)}</nav><a className="nav-cta" href="#contact">Let's talk <span>↗</span></a></header> }
export default Navbar